ERP YOU — Fase 1 · Bitácora de progreso
========================================

2026-06-10 (AFK, sesión desatendida) — Slice #1: Esqueleto + resolución de tenant — COMPLETADO
  - Scaffold Next.js 16 (App Router, TS, src/) + Tailwind v4 vía create-next-app.
  - Prisma 6 + Postgres (docker-compose). Modelo Emisor (id, slug, rfc, razonSocial,
    branding Json, activo, timestamps). Migración init aplicada.
  - Seed: emisor 'demo' (activo) + 'inactivo' (para probar 404).
  - Ruta /f/[slug]: resuelve tenant, renderiza marca (Card shadcn/ui); 404 si no
    existe o está inactivo (lógica pura isRenderableEmisor / nombreParaMostrar).
  - Vitest configurado; 6 tests verdes sobre la lógica pura de tenant.
  - Verificación e2e: /f/demo=200 (con marca), /f/inactivo=404, /f/inexistente=404, /=200.
  - README con pasos de arranque (docker, migrate, seed, dev) y scripts.
  - Decisión técnica: Prisma fijado a v6. Prisma 7 ya no admite `url` en el schema
    (exige prisma.config.ts + driver adapter); se difiere esa migración a un slice futuro.

2026-06-10 (autónomo) — Slice #2: Alta de emisor CLI + facturama-client — CÓDIGO COMPLETADO (5/6)
  - Modelo Emisor extendido: regimenFiscal, cpExpedicion, conceptoDefault (Json),
    ventanaFacturacion (default MISMO_MES), facturamaIssuerRef. Migración aplicada.
  - facturama-client (src/lib/facturama/): auth Basic, switch sandbox/prod por env,
    uploadCsd/listCsds/getCsd/removeCsd. Contrato verificado contra los SDKs oficiales:
    POST api-lite/csds con body { Rfc, Certificate(b64), PrivateKey(b64), PrivateKeyPassword }.
    En Multiemisor el emisor se crea implícitamente al cargar el CSD (no hay createIssuer).
  - config.ts: credenciales desde env (FACTURAMA_USER/PASSWORD/ENV) validadas con Zod.
  - CLI scripts/alta-emisor.ts (npm run emisor:alta): lee .cer/.key → base64, carga CSD,
    upserta Emisor con config fiscal + marca. Validación de args y smoke test OK.
  - 9 tests de contrato (fetch inyectado, sin red): auth header, base URL por ambiente,
    body de uploadCsd, parseo de respuesta, FacturamaError en fallo, get/remove. Total suite: 15 verdes.
  - FALTA (HITL): verificación manual con CSD real en sandbox → necesita credenciales + CSD.

2026-06-10 (autónomo) — Slice #3 (parcial): módulos puros tax + cfdi-builder
  - tax (src/lib/tax.ts): desglosarIva(total, tasa) en centavos; subtotal+iva===total exacto.
    Tests: tabla amplia (1, .99, 116, 100, grandes), valores conocidos, entradas inválidas.
  - cfdi-builder (src/lib/cfdi-builder.ts): arma payload CFDI 4.0 Multiemisor desde
    emisor+receptor+monto+concepto. Defaults fijos: CfdiType=I, PaymentMethod=PUE,
    Currency=MXN, Exportation=01, TaxObject=02, traslado IVA. Nombres de campo verificados
    vs SDK oficial (CfdiMulti/Issuer/Item/Receiver/Tax). Tests: defaults, mapeo emisor/receptor,
    concepto+IVA, reconciliación de centavos, RFC moral(12)/física(13). Suite total: 49 verdes.
  - FALTA del #3 (HITL/integración): createCfdi vs sandbox, modelo Invoice, formulario
    /f/[slug], primer timbrado real (requiere credenciales + CSD del #2).

2026-06-10 (autónomo) — billing-rules (núcleo puro de #6 y #7)
  - src/lib/billing-rules.ts: esRfcValido/tipoPersona (moral 12 / física 13), esCpValido (5 díg),
    perteneceACatalogo, receptorSchema (Zod: RFC+CP+catálogos+email), dentroDeVentana (mismo mes,
    configurable). 15 tests (RFC válidos/ inválidos, CP, fuera de catálogo, ventana dentro/fuera,
    cruce fin de mes, año distinto). Suite total: 64 verdes.
  - Con esto quedan los 4 módulos puros con tests del PRD: tax, cfdi-builder, facturama-client
    (contrato), billing-rules.
  - FALTA de #6/#7 (dependen de #3/UI): seed de catálogos, dropdowns, formulario, restricción
    única (emisorId, folioTicket) + idempotencia (necesitan el modelo Invoice del #3).

2026-06-10 (HITL con el usuario) — Slice #2: CERRADO end-to-end contra sandbox real
  - Cuenta sandbox propia creada (user "youerp"); credenciales en .env (gitignored).
    Las shared pruebas/pruebas2011 ya no sirven (401). Multiemisor (api-lite) OK con la cuenta nueva.
  - CSD de prueba del SAT (EKU9003173C9, pass 12345678a, vigente 2023-2027) descargado a csd/
    (csd/ a .gitignore). Bundle: cdnfacturama.azureedge.net/content/csd-pruebas.zip.
  - La corrida real destapó 2 bugs que los mocks no veían, ya corregidos (commit 4240c42):
      a) uploadCsd: éxito = 200 con cuerpo VACÍO → el cliente devolvía undefined y el CLI
         tronaba en csd.Rfc. Ahora devuelve { Rfc } del input.
      b) No idempotente: re-correr daba 400 "Ya existe un CSD asociado a este RFC" (texto en
         ModelState, no en Message). Nuevo ensureCsd: si ya existe, recupera con getCsd.
      c) tsx no cargaba .env → emisor:alta ahora usa `tsx --env-file=.env`.
  - +3 tests (cuerpo vacío, ensureCsd recupera/propaga). Suite: 67 verdes.
  - Emisor "demo" (EKU9003173C9, régimen 601, CP 64000, concepto "Consumo de alimentos" IVA 16%)
    dado de alta en sandbox y persistido en Postgres → portal /f/demo. Re-ejecución idempotente OK.

2026-06-10 (HITL con el usuario) — Slice #3: CERRADO — primer timbrado real end-to-end
  - Modelo Invoice (emisorId, montos subtotal/iva/total Decimal, receptor, formaPago, estatus,
    uuid @unique, facturamaCfdiId) + relación en Emisor. @@unique([emisorId, folioTicket]) =
    anti-duplicado básico. Migración 20260610170441_invoice_model aplicada.
  - facturama-client.createCfdi (POST api-lite/3/cfdis) + extractUuid (raíz o Complement.TaxStamp).
  - catalogs.ts: subconjunto curado de c_RegimenFiscal/c_UsoCFDI/c_FormaPago (#6 lo amplía).
  - Server Action emitirFactura ('use server'): valida receptor (billing-rules + catálogos),
    desglosa IVA, idempotencia por (emisor, folio), arma CFDI, timbra, persiste Invoice "timbrada",
    devuelve estado con UUID o errores por campo. Formulario cliente (useActionState) + pantalla
    de éxito con el UUID en /f/[slug]. Build de producción OK (ruta dinámica).
  - +4 tests (createCfdi contrato, extractUuid raíz/Complement/ausente). 71 verdes. Lint limpio.
  - TIMBRADO REAL en sandbox verificado por la Server Action misma:
      · submit válido → ok:true, UUID real (p.ej. cc350e97-…), Invoice persistido.
      · re-submit mismo folio → yaExistia:true, mismo UUID (sin doble timbrado).
      · submit inválido → ok:false con error por campo, valores conservados.
  - HALLAZGOS CFDI 4.0 (reglas del SAT, NO bugs): (a) Issuer/Receiver Name debe ser el nombre
    EXACTO del padrón SIN "SA DE CV" (test EKU = "ESCUELA KEMPER URGATE"); (b) DomicilioFiscalReceptor
    de un RFC real se valida contra el padrón del SAT (tuple RFC+nombre+CP+régimen). El camino feliz
    determinista usa Público en General (XAXX010101000 / 616 / S01, DomicilioFiscal = CP de expedición).
    La traducción amable de estos rechazos es el slice #8.

2026-06-10 (AFK, desatendido) — Tanda #4/#6/#7/#8/#9: COMPLETADA, verificada contra sandbox real
  - Migración files_catalogs_error: InvoiceFile (bytea xml/pdf), CatalogoSat (tipo/clave/desc/orden),
    Invoice += fechaTicket, errorPac. Seed siembra 15 claves de catálogo.
  - #8 pac-errors.ts (puro): extraerDetallePac (lee ModelState) + traducirErrorPac (patrones →
    mensajes accionables en español). Invoice estatus "error" + errorPac; reintento del mismo folio
    sí timbra. +11 tests.
  - #4 facturama-client.getXml/getPdf (GET cfdi/{xml,pdf}/issuedLite/{id} → base64, verificado por
    curl real). guardarArchivos best-effort tras timbrar (no tumba el UUID si falla la descarga).
    Ruta GET /factura/[invoiceId]/[tipo] sirve los bytes con Content-Type + filename=UUID.
  - #6 catálogos desde BD: catalogs.server.ts loadCatalogos/clavesValidas; dropdowns y validación
    (billing-rules) alimentados desde CatalogoSat. catalogs.ts queda como fuente de la siembra.
  - #7 ventana mismo-mes aplicada (dentroDeVentana con fechaTicket+ventanaFacturacion del emisor);
    folio único (emisorId, folioTicket): timbrada bloquea, error se reintenta. Captura folio+fecha+monto.
  - #9 preview de desglose en vivo (tax) en el formulario; pantalla de éxito pulida (resumen receptor,
    UUID, descargas PDF/XML); consulta read-only `npm run facturas:list -- --slug demo`.
  - Verificación real (Server Action contra sandbox): happy path → UUID + XML(<?xml,4.8KB)/PDF(%PDF,54KB)
    conservados; idempotencia (yaExistia, mismo UUID); ventana fuera de mes bloqueada; rechazo del PAC
    (CP equivocado) → mensaje traducido + Invoice "error"; reintento mismo folio → timbra y limpia errorPac.
    Ruta de descarga: PDF/XML 200 con Content-Type correcto, 404/400 en faltante/tipo inválido.
  - 82 tests verdes. Lint, typecheck y build de producción limpios. (commit pendiente)

PENDIENTE:
  - Slice #5 (HITL): entrega por correo (Resend) — requiere API key + dominio verificado. Único
    slice de F1 que falta; el "aviso de correo" en la pantalla de éxito queda como placeholder.
  - Nota producto: receptor con RFC real se valida contra el padrón del SAT (nombre+CP+régimen). El
    happy path de demo usa Público en General (XAXX010101000/616/S01). El emisor debe darse de alta
    con el nombre EXACTO del padrón sin "SA DE CV".
